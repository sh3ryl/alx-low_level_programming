#include <stdio.h>

/**
 * main - serves as starting point forprogram execution
 *
 * Return: always 0
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle);
	return (0);
}
